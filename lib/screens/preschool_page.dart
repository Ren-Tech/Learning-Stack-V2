import 'package:flutter/material.dart';

class PreschoolPage extends StatelessWidget {
  const PreschoolPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Pre-School Page Content'));
  }
}

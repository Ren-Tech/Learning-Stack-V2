import 'package:flutter/material.dart';

class ALevelPage extends StatelessWidget {
  const ALevelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('A-Level Page Content'));
  }
}
